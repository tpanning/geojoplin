# Features
* Allow the user to change the location of a Joplin note by dragging the icon
* Allow the user to change the location of a Joplin note by typing a location (e.g., "San Francisco" or "Washington Monument") and getting a list of places to choose from


# Technical
* Re-scan port numbers if the Joplin application restarts and 

# Future
* Allow the user to save and load layer settings to a Joplin note
* If a note has a GeoJSON attachment, render that instead
* Same, but for KML
* Mobile app